{
  "name": "@eamvp/llm",
  "version": "0.1.0",
  "private": true,
  "description": "命理+心理 双声部解读层 —— provider 无关（OpenAI 兼容），默认 MiniMax-M3，可切 DeepSeek/任意兼容端点",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@eamvp/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
